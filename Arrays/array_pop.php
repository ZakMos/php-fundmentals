<?php
$authors = array(
  "Charles Dickens",
  "Jane Austin",
  "William Shakespeare",
  "Mark Twain"
);

  // mixed array_pop(array & $string)
  $lastValue = array_pop($authors);
  echo $lastValue; // will display the last value from array
  print_r($authors); // will display all arrays

 ?>
